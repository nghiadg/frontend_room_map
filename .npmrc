# Enable multi-platform installation for sharp (required for Vercel/Linux deployment)
# See: https://sharp.pixelplumbing.com/install#cross-platform

# Install optional dependencies
optional=true

# Force install for both platforms
# This ensures sharp works on macOS (local dev) and Linux (Vercel)
libc=glibc
